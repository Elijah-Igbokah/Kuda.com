import React from "react";

import "./partner-icon.styles.scss";





const PartnerIcon = ({children}) => {
    return(
        <div className="container">
            {children}
        </div>
    );
};

export default PartnerIcon;